# nftables / UFW live ruleset summary — 2026-08-10_2321

Source: `sudo nft list ruleset` (full dump: `2026-08-10_2321_nft.txt` if re-dumped;
content verified from operator paste same session).

## High-level policy (LIVE)

| Hook | Policy | Notes |
|------|--------|-------|
| ip filter INPUT | **drop** | UFW active |
| ip filter OUTPUT | **accept** | |
| ip filter FORWARD | **drop** | Docker chains first, then UFW |
| ip6 filter INPUT | **drop** | |
| ip6 filter OUTPUT | **accept** | |
| ip6 filter FORWARD | **drop** | |

## UFW user rules

- `ufw-user-input` — **empty** (no explicit allows)
- `ufw-user-output` — empty
- `ufw-user-forward` — empty
- Same for IPv6 `ufw6-user-*`

Interpretation: pure default-deny inbound; only built-in `before` exceptions apply.

## Built-in inbound accepts (IPv4 `ufw-before-input`)

- `lo` accept
- conntrack related,established
- invalid → log+drop
- ICMP: dest-unreach, time-exceeded, param-problem, **echo-request** (ping)
- UDP sport 67 dport 68 (DHCP client replies)
- Multicast mDNS `224.0.0.251:5353`
- Multicast SSDP `239.255.255.250:1900`
- Then jump `ufw-user-input` (empty) → after → policy drop

## After-input (noise → drop)

NetBIOS/SMB/DHCP server ports jump to skip-to-policy (drop):
- UDP 137/138, TCP 139/445, UDP 67/68 (server roles)

Counters at capture (approx from paste):
- NetBIOS-ish noise seen (137/138 packets > 0) — correctly dropped
- UFW BLOCK logging had traffic (inbound probes / unsolicited)

## Docker (ip filter + nat)

- DOCKER / DOCKER-FORWARD / isolation present
- Bridges: `docker0`, `br-283c3d8ea7b7`
- Inbound to docker bridges from non-bridge → drop
- NAT masquerade for 172.17.0.0/16 and 172.19.0.0/16
- All Docker forward counters **0** (no container traffic)

## IPv6 notes

- Type-0 routing header drop
- NDP / MLD essentials accepted (needed for IPv6 on wlan0)
- DHCPv6 link-local 547→546 accepted
- mDNS/SSDP multicast accepts present
- user-input empty; residual packets hit UFW BLOCK log (11 packets in after-logging)

## Security assessment

**Good:** Live policy matches config — deny in/forward, no custom open ports.
**Info:** Echo-request allowed (host is pingable on L3 if path exists).
**Info:** mDNS/SSDP multicast accepts are UFW defaults; low risk with no avahi/ssdp listeners.
**OK:** SMB/NetBIOS not open; noise dropped.
