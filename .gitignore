# Data / evidens (lokalt, ikke git)
logs/
*.log
scans/
reports/*_scan.md

# Secrets — aldri commit
.env
.env.*
config/env
*.token
**/api.token
config.toml
!config/kalived.toml.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
.DS_Store
*~

# AIDE checksum is OK in baselines/; DB is under /var/lib/aide (utenfor repo)

# Bundled Android binaries (large)
tools/adb
tools/fastboot
